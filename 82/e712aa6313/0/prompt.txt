提交代码

---

修改 agents.md，git 提交等简单任务，不需要通过planning-with-files 这种重量级的plan skill

---

参考如下信息优化本项目AGENTS.md及子目录AGENTS.md：

深度理解本项目源码，完全对齐 OpenAI Codex + agents.md 官方模板和最佳实践， https://agents.md/、https://github.com/search?q=path%3AAGENTS.md+NOT+is%3Afork+NOT+is%3Aarchived&type=code、https://github.com/openai/codex/blob/main/AGENTS.md、  https://developers.openai.com/codex/guides/agents-md。

融合项目现有分层结构（root → server/apps/web/e2e 子 AGENTS.md） - 强化 **YOU MUST / NEVER / IMPORTANT** 关键词（Codex 遵守率最高） - 完整纳入 CONTRIBUTING.md 的 worktree / .env / 数据库隔离规则 - 增加 Verification、Security、Gotchas（官方强烈推荐） - 保持简洁
## Project Overview
[一句话描述项目：技术栈、架构特点、核心目标]
## Key Commands (YOU MUST use these exact commands)
- 安装依赖：``
- 开发服务器：``
- 构建：``
- 类型检查：``
- Lint + 自动修复：``
- 测试（全部）：``
- 测试单个文件：``
- 预览构建：``
## Code Style (IMPORTANT - NEVER violate)
- 使用 TypeScript strict mode
- 优先函数式组件 + hooks，禁止 class 组件
- 所有新功能必须写单元测试
- 禁止 console.log（使用 logger 模块）
- 导入顺序：React → 第三方 → 内部模块（按字母排序）
- 禁止 any 类型，必须显式定义
## Repository Structure

## Workflow & Verification (Done 定义)
- 每次修改代码后**必须**运行 typecheck + lint + 相关测试
- PR 前必须通过所有检查
- 完成任务的标准：代码通过 lint + test + typecheck + 功能手动验证通过
- 提交前运行 `pnpm lint && pnpm test`

## Constraints & Do-Not Rules (NEVER)

- 禁止直接修改生产环境的 API Key / 密钥

- 禁止在代码中硬编码敏感信息

- 禁止使用已废弃的 API 或库 。 --- ### **推荐直接复制 → 替换项目根目录 `AGENTS.md`** ```markdown # AGENTS.md - Authoritative Instructions for Codex (Multica Project) ## Project Overview (YOU MUST read first) Multica 是 AI-native 任务管理平台，将 Claude Code / Codex 等 coding agents 变成真正队友。 核心能力：把 GitHub Issue 直接 assign 给 agent，agent 自动认领、写代码、报告 blocker、更新状态、提交 PR。 - **Frontend**: Next.js 16 App Router（apps/web/） - **Backend**: Go（Chi router + sqlc + gorilla/websocket + PostgreSQL 17 + pgvector）（server/） - **Agent Runtime**: 本地 daemon 自动检测 claude / codex CLI 并执行任务 - **测试**: Playwright E2E（e2e/） - **架构原则**: 保持现有架构，不要引入不必要抽象；优先删除废弃路径而非双写 **分层规则（Official nearest-wins）** Codex 会自动读取**最近的** AGENTS.md： - Root（本文件）：全局规则 - `server/AGENTS.md`：Go backend、daemon、sqlc、migrations - `apps/web/AGENTS.md`：Next.js 前端 - `e2e/AGENTS.md`：Playwright 测试 子目录文件**覆盖** root 规则。 ## Setup & Development Commands (YOU MUST use these) **核心 Make 命令（项目官方推荐）**： - `make setup` / `make setup-main` → 首次安装 - `make start` / `make start-main` → 启动主环境 - `make stop` / `make stop-main` → 停止 - `make check` / `make check-main` → 完整验证（typecheck + test + lint） - `make test` → 所有测试 - `make build` → 构建 **Worktree 专用（IMPORTANT - 必须遵守）**： - `make worktree-env` → 生成 `.env.worktree` - `make setup-worktree` / `make start-worktree` / `make check-worktree` - **NEVER** 把 `.env` 复制到 worktree（会导致数据库冲突） - 每个 worktree 使用独立 PostgreSQL 数据库（自动隔离） **其他常用**： - `pnpm install`、`pnpm build`、`pnpm typecheck`、`pnpm test` - `make db-up` / `make db-down`（共享 PostgreSQL 容器） ## Code Style & Working Rules (NEVER violate) - 先探索现有代码路径，严格匹配当前架构 - 修改执行流程时，必须同步更新对应 AGENTS.md 和测试 - **NEVER** 手动编辑 `server/pkg/db/generated/`（sqlc 生成） - 代码注释必须全英文 - **NEVER** 主动添加兼容 shim、双写逻辑、fallback 或遗留适配器 - 除非任务明确要求，否则不要做大范围重构 - Prefer 删除废弃路径，而非同时保留新旧行为（项目尚未正式上线） ## Testing & Verification (Done 的定义) - 任何代码修改后 **必须** 运行对应窄域验证，再跑 `make check` - 用户未明确要求时，不要默认跑重度验证 - 完整仓库验证命令：`make check`（包含 TS typecheck、Go test、Playwright E2E） - 测试通过 + 功能手动验证 = 任务完成 ## PR & Commit 规范 - 使用 Conventional Commits：`feat(web): ...`、`fix(daemon): ...`、`refactor(server): ...` 等 - PR 前必须通过 `make check` - Commit 保持 atomic，按逻辑意图分组 ## Security & Gotchas (IMPORTANT) - **NEVER** 在代码中硬编码敏感信息（JWT_SECRET、数据库密码等） - 所有环境变量必须来自 `.env` / `.env.worktree` 或 Docker secrets - Agent daemon 依赖系统 PATH 中存在 `claude` / `codex` CLI - PostgreSQL 容器停止后数据库卷仍保留；彻底重置请用 `docker compose down -v` + 重新 setup - Worktree 环境隔离严格依赖 `.env.worktree` 文件，**NEVER** 混用 `.env` ## Dev Environment Tips - 优先使用 `rg` 搜索 - 保持 root AGENTS.md 简短，子系统细节放入对应子 AGENTS.md - 如果修改 Rust/Go/TS 依赖，务必同步 lock 文件（pnpm / go mod） --- **使用建议**： 1. 直接替换根目录 `AGENTS.md`（已包含项目现有全部精华 + 官方最佳实践） 2. 子目录的 `server/AGENTS.md`、`apps/web/AGENTS.md`、`e2e/AGENTS.md` 保持不变（或按需同步风格） 3. 全局默认可额外放 `~/.codex/AGENTS.md`（可选） 4. 让 Codex 执行一次 `"Summarize the current instructions."` 验证是否正确加载

- 永远不要 commit 未通过 lint 的代码
- 禁止在没有测试覆盖的情况下修改核心业务逻辑

## PR & Commit 规范
- Commit message 使用 Conventional Commits（feat/fix/refactor 等）
- PR Title 格式：`[模块] 简要描述`
- 每次 PR 必须附上测试通过截图或命令输出

## Gotchas（项目特有坑）

[在这里写你项目常见的坑，例如数据库迁移命令、环境变量等]

@import docs/architecture.md # 可选：模块化引用其他文档

---

提交暂存区代码